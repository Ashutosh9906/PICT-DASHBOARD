<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - PICT Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Sharp" rel="stylesheet">
</head>
<style>
    :root {
        --colorI: #f7f2eb;
        --colorII: #ffffff;
        --colorIII: #dde3ff;
        --colorIV: #bad6eb;
        --colorV: #7096d1;
        --colorVI: #334eac;
        --colorVII: #081f5c;
        --error-red: #dc3545;
        --error-light: #f8d7da;
        --shadow: 0 4px 20px rgba(0, 0, 0, 0.07);
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

    body {
        background: linear-gradient(135deg, var(--colorIV) 0%, var(--colorV) 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        overflow-x: hidden;
    }

    .error-container {
        background-color: var(--colorII);
        border-radius: 24px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        padding: 3rem;
        max-width: 600px;
        width: 100%;
        text-align: center;
        animation: slideIn 0.6s ease-out;
        position: relative;
        overflow: hidden;
    }

    @keyframes slideIn {
        from {
            opacity: 0;
            transform: translateY(-30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .error-container::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 6px;
        background: linear-gradient(90deg, var(--colorVI), var(--colorV));
    }

    .error-icon {
        width: 120px;
        height: 120px;
        margin: 0 auto 2rem;
        background: linear-gradient(135deg, var(--error-light) 0%, #fff 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: pulse 2s ease-in-out infinite;
        box-shadow: 0 4px 20px rgba(220, 53, 69, 0.2);
    }

    @keyframes pulse {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.05);
        }
    }

    .error-icon .material-icons-sharp {
        font-size: 4rem;
        color: var(--error-red);
    }

    .error-code {
        font-size: 6rem;
        font-weight: 700;
        color: var(--colorVI);
        line-height: 1;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
    }

    .error-title {
        font-size: 2rem;
        font-weight: 600;
        color: var(--colorVII);
        margin-bottom: 1rem;
    }

    .error-message {
        font-size: 1rem;
        color: #666;
        line-height: 1.6;
        margin-bottom: 2rem;
        padding: 0 1rem;
    }

    .error-details {
        background-color: var(--colorIII);
        border-left: 4px solid var(--colorVI);
        padding: 1rem 1.5rem;
        margin-bottom: 2rem;
        border-radius: 8px;
        text-align: left;
    }

    .error-details p {
        margin: 0.5rem 0;
        font-size: 0.9rem;
        color: #555;
        word-wrap: break-word;
    }

    .error-details strong {
        color: var(--colorVII);
        font-weight: 600;
    }

    .button-group {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn {
        padding: 0.8rem 2rem;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        border: none;
        outline: none;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--colorVI), var(--colorV));
        color: white;
        box-shadow: 0 4px 15px rgba(51, 78, 172, 0.3);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(51, 78, 172, 0.4);
    }

    .btn-secondary {
        background-color: var(--colorIII);
        color: var(--colorVI);
    }

    .btn-secondary:hover {
        background-color: var(--colorV);
        color: white;
        transform: translateY(-2px);
    }

    .btn .material-icons-sharp {
        font-size: 1.2rem;
    }

    /* Floating elements decoration */
    .floating-shape {
        position: absolute;
        border-radius: 50%;
        opacity: 0.1;
        animation: float 6s ease-in-out infinite;
    }

    .shape-1 {
        width: 80px;
        height: 80px;
        background-color: var(--colorVI);
        top: 10%;
        left: 5%;
        animation-delay: 0s;
    }

    .shape-2 {
        width: 60px;
        height: 60px;
        background-color: var(--colorV);
        bottom: 15%;
        right: 8%;
        animation-delay: 2s;
    }

    .shape-3 {
        width: 100px;
        height: 100px;
        background-color: var(--colorVI);
        top: 60%;
        left: 10%;
        animation-delay: 4s;
    }

    @keyframes float {
        0%, 100% {
            transform: translateY(0) rotate(0deg);
        }
        50% {
            transform: translateY(-20px) rotate(180deg);
        }
    }

    /* Responsive Design */
    @media screen and (max-width: 768px) {
        .error-container {
            padding: 2rem 1.5rem;
        }

        .error-code {
            font-size: 4rem;
        }

        .error-title {
            font-size: 1.5rem;
        }

        .error-message {
            font-size: 0.9rem;
        }

        .error-icon {
            width: 100px;
            height: 100px;
        }

        .error-icon .material-icons-sharp {
            font-size: 3rem;
        }

        .button-group {
            flex-direction: column;
        }

        .btn {
            width: 100%;
            justify-content: center;
        }
    }

    @media screen and (max-width: 480px) {
        .error-container {
            padding: 1.5rem 1rem;
        }

        .error-code {
            font-size: 3rem;
        }

        .error-title {
            font-size: 1.2rem;
        }

        .error-icon {
            width: 80px;
            height: 80px;
            margin-bottom: 1.5rem;
        }

        .error-icon .material-icons-sharp {
            font-size: 2.5rem;
        }

        .error-details {
            padding: 0.8rem 1rem;
        }

        .error-details p {
            font-size: 0.85rem;
        }

        .btn {
            padding: 0.7rem 1.5rem;
            font-size: 0.9rem;
        }
    }
</style>

<body>
    <!-- Floating decorative shapes -->
    <div class="floating-shape shape-1"></div>
    <div class="floating-shape shape-2"></div>
    <div class="floating-shape shape-3"></div>

    <div class="error-container">
        <div class="error-icon">
            <span class="material-icons-sharp">error_outline</span>
        </div>
        
        <!-- Change error code dynamically: 404, 500, 403, etc. -->
        <h1 class="error-code">500</h1>
        
        <!-- Change error title -->
        <h2 class="error-title">Oops! Something went wrong</h2>
        
        <!-- Error message -->
        <p class="error-message">
            We encountered an unexpected error. Please try again later or contact support if the problem persists.
        </p>

        <!-- Optional: Error details (can be hidden in production) -->
        <!-- <% if (errorDetails) { %>
        <div class="error-details">
            <p><strong>Error Details:</strong></p>
            <p><%= errorDetails %></p>
        </div>
        <% } %> -->

        <!-- Action buttons -->
        <div class="button-group">
            <a href="/messages" class="btn btn-primary">
                <span class="material-icons-sharp">home</span>
                Go to Dashboard
            </a>
            <button onclick="history.back()" class="btn btn-secondary">
                <span class="material-icons-sharp">arrow_back</span>
                Go Back
            </button>
        </div>
    </div>

    <script>
        // Optional: Auto-redirect after certain time
        // setTimeout(() => {
        //     window.location.href = '/';
        // }, 10000); // Redirect to dashboard after 10 seconds
    </script>
</body>

</html>